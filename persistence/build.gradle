plugins {
    id "java-library"
}

dependencies {
    api "org.springframework.boot:spring-boot-starter-data-jpa:${springDataJpaVersion}"

    runtimeOnly "com.microsoft.sqlserver:mssql-jdbc:${sqlserverVersion}"
}