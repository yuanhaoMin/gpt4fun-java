dependencies {
    implementation project(":chamber")
    // TODO: Stop using discord until the websocket disconnect issue is fixed
    // implementation project(":discord")
}

bootJar {
    enabled = true
}

jar {
    enabled = false
}